package library.commandrunner.add;

import java.util.List;

class AddCustomerCommand extends AddCommand {

    public AddCustomerCommand(List<String> commandStrings) {
        super(commandStrings);
    }
}
