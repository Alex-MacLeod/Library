package library.commandrunner.add;

import java.util.List;

public class AddLibraryItemCommandRunner extends AddCommandRunner {

    public AddLibraryItemCommandRunner(List<String> commandStrings) {
        super(commandStrings);
    }
}
